spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
        # search-locations: "classpath:/config"
        search-locations: "C:/Users/Luecha/Documents/Dev/project_springboot/Master-Microservices-with-SpringBoot/config"

server:
  port: 8071